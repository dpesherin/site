<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ForgotPass</title>
</head>
<body>
    <style>
        .wrapper{
            width: 100%;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            background-color: #fff;
        }
        .content{
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            gap: 15px;
            align-items: center;
            padding: 24px;
        }
        h4.modern{
            color: darkblue;
            font-size: 18px;
            font-weight: 600;
        }
        h5.modern{
            color: darkblue;
            font-size: 14px;
            font-weight: 600;
        }
        p.modern{
            color: rgba(128, 128, 128, 0.45);
            font-size: 12px;
        }
        p.code{
            color: darkblue;
            font-size: 16px;
            font-weight: 600;
        }
        a.modern{
            color: white;
            padding: 12px 24px;
            background-color: darkblue;
            border-radius: 5px;
            font-size: 14px;
            text-decoration: none;
        }
        .d-flex{
            display: flex;
            justify-content: flex-start;
        }
        .info-label{
            color: rgba(128, 128, 128, 0.85);
            font-weight: 600;
            font-size: 10px;
        }
        .info-text{
            color: rgba(128, 128, 128, 0.65);
            font-size: 10px;
        }
    </style>
    <div class="wrapper">
        <div class="content">
            <h4 class="modern">Код для восстановления</h4>
            </br>
            <div class="clientInfo">
                <h5 class="modern">Данные о запросившем клиенте:</h5>
                <div class="d-flex">
                    <span class="info-label">IP: </span><p class="info-text"><%= ip %></p>
                </div>
                <div class="d-flex">
                    <span class="info-label">Country: </span><p class="info-text"><%= country %></p>
                </div>
                <div class="d-flex">
                    <span class="info-label">City: </span><p class="info-text"><%= city %></p>
                </div>
            </div>
            <p class="modern">
                Ваш код для восстановления пароля:
            </p>
            <p class="code">
                <%= code %>
            </p>
            <a class="modern" href="<%= link %>">Восстановить пароль</a>
        </div>
    </div>
</body>
</html>